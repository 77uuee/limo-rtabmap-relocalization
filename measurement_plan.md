# RTAB-Map 재위치추정 성공률 측정 계획 (초안)

과제 출처: slam_rtabmap_2.pptx 17~19p
목표: 카메라 전용 RTAB-Map 지도에서, 조건이 바뀔 때 재위치추정이
얼마나 버티는지 정량 측정한다. **지도는 한 번만 만들고 고정한다.**

## 0. 판정 기준 (확정 — 2026-08-30)

**정답 좌표 기반 객관 판정.** 각 지점은 테이프로 고정되므로 정답 자세를
안다. C1 첫 회차에서 수렴이 좋을 때의 좌표를 그 지점의 정답으로 기록해 둔다.

- **성공**: `/rtabmap/localization_pose` 첫 발행 자세가
  정답에서 **위치 0.5 m 이내 + 방향 30° 이내**
- **오탐**: 발행됐지만 위 허용치를 벗어남 (미수렴보다 나쁨 — 별도 분류)
- **미수렴**: 관측 종료까지 발행 없음

**2단계 프로토콜** (회차당 최대 60초):
- Phase A (0~20초): 정지 상태로 대기
- Phase B (20~60초): 미수렴 시 제자리 저속 회전 허용
- 기록: 수렴 시각(초)과 Phase.
- **대표 수치 = "60초 내(회전 포함) 성공률 + 수렴 시간"** (2026-08-30 파일럿 후 변경).
  보조 수치 = 20초 내(정지) 성공률.
  이유: 파일럿에서 가장 쉬운 P1조차 정지 수렴 실패. RTAB-Map은 매핑 때
  지나가며 본 방향(viewpoint)의 화면만 DB에 있어, 테이프 방향이 매핑 경로
  방향과 다르면 회전으로 그 시점을 찾아야 수렴함 (P1 1·2회차 모두 102~113°
  에서 수렴 — 매핑 주행 방향으로 추정). 수렴 yaw 기록으로 사후 분석 가능.
- 위치추정 launch에 `--RGBD/LinearUpdate 0 --RGBD/AngularUpdate 0` 적용됨
  (기본값이면 정지 중 루프 클로저 재시도 자체를 안 함 → Phase A 측정 불가)

이렇게 하면 결과물이 3층이 된다:
1) 조건별 성공률 표 (헤드라인)
2) 시간 허용치별 성공률 곡선 (10/20/30/60초에서 잘라 그림)
3) 성공 회차의 위치 오차 분포 (성공해도 얼마나 정확했나)

주의: RTAB-Map은 AMCL과 달라 파티클 분산이 아니라 **루프 클로저 성사 여부**가
본질이다. localization_pose가 아예 안 나오면 "한 번도 못 알아본 것".
covariance는 판정에 쓰지 않고 참고 기록만 한다 (AMCL sigma 사례처럼
자신감 수치는 실제 정확도를 보장하지 않음).

## 0.1 정답 좌표 출처 — 테이프로 통일 (2026-09-01 변경)

**변경**: 회차 정답을 "시작 시점 tf 스냅샷"에서 **바닥 테이프 좌표(points.csv)** 로 변경.
`auto_trial.py --truth` 기본값 `points`. (`snap`은 옵션으로 남김)

**근거 — 2026-08-31 17:55 사건**: 로봇은 P1에 서 있는데 rtabmap 믿음은
P7(4 m 밖)으로 점프해 있었다 (P1↔P7 가짜 루프 클로저, p1_rest.log).
스냅샷을 정답으로 쓰면 ① 이런 상태에서 회차를 시작할 수 없고(0.7 m 가드),
② 가드를 풀면 틀린 좌표가 정답이 되어 오차 0 cm "성공"으로 기록된다 —
측정 대상이 스스로 정답을 정하는 순환논증. 재려는 실패 모드를 정확히 못 본다.

**테이프 좌표가 정답으로 성립하는 근거**:
- 수렴이 확실히 좋을 때 1회 등록 후 고정 (회차와 독립)
- 지점 21쌍 상호 거리를 v1 좌표와 교차검증 — 최대 차이 16 cm (§2)
- 조이스틱 정렬 오차(5~10 cm 추정)는 판정 허용치 0.5 m 대비 작고,
  성공률을 **낮추는 쪽으로만** 작용 (보수적 측정)

**병행 기록**: 스냅샷은 계속 기록한다 (`snap_*`, `err_*_snap_*` 열).
판정에는 쓰지 않고, 두 기준으로 계산해도 결론이 같은지 보이는 민감도 분석용.
믿음이 테이프에서 0.7 m 이상 벗어난 채 시작한 회차는 note에 `snapjump` 표시
→ 가짜 루프 클로저 발생 기록 자체가 결과물 소재 (실패 지점 지도의 오탐 사례).

**기존 유효 4회(P7×3, P1×1)는 기준 통일을 위해 재측정한다.**
기존 행은 truth_src=snap 으로 표시해 보존 (results.csv 스키마 자동 이행됨).

**회차 전 1초 점검**: `python3 ~/reloc_test/auto_trial.py --where`
→ 현재 믿음과 각 등록 지점까지의 거리 출력. 점프 여부를 즉석에서 확인.

## 0.5 사고 기록 — 기준 지도 유실 (2026-08-30 발생, 08-31 확인)

**증상**: `~/.ros/limo_rtabmap_camera.db` 가 120 KB / **Node 0개**.
스키마만 남고 지도는 사라졌다. 백업 없음 → **재매핑 외에 복구 방법 없음**.

**경위** (`~/lab_logs_nav_launch.log` 로 확인):
```
14:03:29  P1 C1 #3 측정 정상 종료 (WM=894)
14:03:51  [rtabmap] rtabmap: Reset          ← /rtabmap/reset 호출
14:03:52  WARN  The working memory is empty ... (local map=0, WM=0)
14:11     launch 재시작 시 rtabmap 정상 종료 → 빈 메모리가 DB에 저장됨
          (DB mtime 14:11, 이후 모든 실행이 0노드 DB를 물고 돎)
```

**원인**: rtabmap은 localization 모드에서도 **종료 시 현재 메모리를 DB에 쓴다**.
`/rtabmap/reset` 으로 WM이 비워진 상태에서 정상 종료되면 그 빈 상태가 저장된다.
`--delete_db_on_start` 같은 것을 쓰지 않아도 이렇게 지워진다.

**당시 가드가 못 막은 이유**: navigation launch의 검사가 `파일 크기 > 1024 B`
뿐이었다. 빈 DB도 120 KB라 통과했고, 그대로 무의미한 측정을 계속할 뻔했다.

**대응 (2026-08-31 적용 완료)**:
1. `tools/save_master.sh` — 매핑 직후 원본을 `~/maps/rtab_camera_master.db` 로
   복사하고 **chmod 444**. 실수로 열려도 rtabmap이 덮어쓸 수 없다.
2. `auto_trial.py` — 측정은 원본에 붙지 않는다. 매 회차 원본을
   `/tmp/reloc_work.db` 로 복사하고 launch에 `database_path:=` 로 그 사본을 준다.
   시작 시 `check_master()` 가 원본 노드 수를 확인, 0이면 실행 거부.
3. navigation launch의 `validate_database()` — 크기 대신 **`SELECT COUNT(*)
   FROM Node`** 로 검사. 0노드면 launch 자체가 뜨지 않는다.
4. `results.csv` 에 **`map` 열** 추가. 재매핑하면 좌표계가 바뀌어 이전 데이터와
   섞을 수 없으므로 지도 태그를 함께 기록한다 (`--map v2`).

**유실 데이터**: map v1 기준 측정치(P1 C1 3회)와 정답 좌표 7개는 폐기.
`tools/results_mapv1_lost.csv`, `tools/points_mapv1_lost.csv` 로 보관만 함.
바닥 테이프는 그대로이므로 **지점 자체는 유지**되고, 좌표만 다시 등록하면 된다.

## 1. 기준 지도 (1회만)

- launch: `ros2 launch wego limo_rtabmap_mapping_camera_only.launch.py`
- DB: `~/.ros/limo_rtabmap_camera.db` (camera_only 전용 경로. LiDAR 병용 DB와 분리)
- 구역: 강의실 안쪽 절반 권장. 이유:
  - 복도는 무늬 없는 벽 위주라 기준 지도부터 부실해질 위험
  - 과제는 "지도 신뢰도" 측정이므로 기준 지도는 **잘 만들어져야** 함 (걸림돌 4번)
- 요령: 같은 곳을 짧게 여러 번 재방문 (PPT 8p — 루프 클로저는 재방문에서만 생김)
- 매핑 직후 `rtabmap-databaseViewer ~/.ros/limo_rtabmap_camera.db`로
  루프 클로저 개수·그래프 품질 확인하고 시작할 것
- **매핑이 끝나면 바로** `~/reloc_test/save_master.sh` 실행 (0.5절 참고).
  이걸 안 하면 지도가 또 날아가도 복구 수단이 없다.
- 노드 수 즉석 확인 (로봇에 sqlite3 CLI 없음 → python3 사용):
  `python3 -c "import sqlite3;print(sqlite3.connect('/home/wego/.ros/limo_rtabmap_camera.db').execute('select count(*) from Node').fetchone())"`

## 2. 측정 지점 5~8곳 (매핑 후 확정)

**map v2 좌표로 재등록 완료 (2026-08-31. 그림: measurement_points_v2map.png)**
테이프(물리적 지점)는 2026-08-30 것 그대로. 지도가 v2로 바뀌어 좌표만 재등록.
좌표 원본은 tools/points.csv (v1 좌표는 points_mapv1_lost.csv 로 보관).

| # | v2 좌표 (x, y, yaw°) | 난이도 | 내용 |
|---|---------------------|--------|------|
| P1 | (-1.83, 4.37, 61) | 쉬움 | 강의실 중앙 통로, 책상·장비 방향 — 기준값 |
| P2 | (5.67, 1.68, -16) | 중간 | **창문 벽 정면** — C2 역광 테스트 핵심 |
| P3 | (1.17, 0.06, -82) | 어려움 | 무늬 없는 벽 |
| P4 | (-5.93, 7.58, 91) | 중간 | 문 통과 직후 복도 초입 |
| P5 | (0.52, 8.79, 150) | 중간 | 복도 중간 |
| P6 | (-11.18, 9.35, -29) | 어려움 | 복도 끝 — 반복 무늬 |
| P7 | (-6.19, 4.62, -177) | 어려움 | 강의실 북쪽 경계 — 관측 부족 구역 방향 |


**P7 재등록 (2026-09-01)**: 실습 구역 정리로 P7 테이프 유실 → Nav2로 구좌표 부근 이동 후 현 위치에 테이프 재설치, 좌표를 수렴 상태 tf로 재등록 (-6.049, 4.515, 171.7°). 구좌표(-6.185, 4.621, -176.7°)에서 17 cm / 12° 이동. 원 등록 절차와 동일한 방식이므로 유효. 8/31 P7 데이터(truth_src=snap)는 구테이프 기준이라 어차피 재측정 대상.

재등록 검증 (2026-08-31): 지점 21쌍의 상호 거리를 v1 좌표와 비교 —
**최대 차이 16 cm** (좌표계가 달라도 테이프 간 거리는 불변이어야 함).
지도 왜곡 + 등록 오차 합이 이 수준이면 판정 허용치 0.5 m 대비 충분히 작음.

공간 정보 (사용자 확인): 창문은 출발 방향 기준 오른쪽 = 남쪽 벽에 여러 개.
출입문 2개 중 하나는 사용 안 함, 통행은 P4 쪽만 (경사 + 턱 있음).

- 각 지점 바닥에 테이프로 위치+방향 표시 (걸림돌 2번: 출발 자세 통일)
- 테이프 위치 잡는 법: 위치추정 모드 켠 채 로봇을 손으로 옮기며
  `ros2 run tf2_ros tf2_echo map base_link` 값이 목표 좌표와 맞는 자리에 표시

## 2.5 경로계획 전용 지도 (nav_map) — 2026-08-31 도입

**문제**: 자율주행 실습 구간의 회색 펜스를 depth 카메라가 조각으로만 인식
(우산 등 임시 물체는 오히려 지도에 박힘). 이 스택은 Nav2 static layer가
rtabmap이 DB에서 생성하는 /map을 직접 구독하므로, pgm을 그려도 반영 안 됨.

**해결**: 경로계획용 지도를 분리.
```
rtabmap DB → 재위치추정 (실험 대상 — 손대지 않음)
~/maps/rtab_camera_nav.yaml → map_server(/map_nav) → Nav2 global costmap
```
- launch에 `nav_map:=<yaml>` 인자 추가. auto_trial.py는 로봇에
  `~/maps/rtab_camera_nav.yaml`이 있으면 자동으로 넘김
- 재위치추정은 시각 특징점만 쓰므로 nav_map 편집은 측정에 영향 없음
- **함정**: RewrittenYaml은 원본 yaml에 있는 키만 바꿀 수 있음. static_layer에
  `map_topic` 키가 없어서 재작성이 조용히 무시됐었음 →
  diff_navigation_params.yaml에 `map_topic: /map` 명시로 해결 (.bak11-20260831)
- 검증 루틴: (1) 로봇 반경 팽창 후 P1 기준 flood fill로 7지점 도달성,
  (2) /global_costmap/costmap에서 그린 벽 표본 좌표가 100(lethal)인지
- nav_map 편집: 맥북에서 `map_editor.py rtab_camera_nav.yaml -o rtab_camera_nav`
  → scp로 로봇 ~/maps/에 업로드

**지도 계보**: v2(첫 매핑 1611노드) → v2.1(펜스 이어매핑 1982노드, 원본
rtab_camera_master.db) + rtab_camera_nav(v2.1에 펜스·복도끝 손그림, planner 전용).
v2.1 이어매핑 후 P1 스팟 체크 오차 0.04 m/1° → v2 정답 좌표 그대로 유효.

## 2.6 2026-08-31 세션 기록 — C1 측정 개시 (강의실 4/12회)

### 확보한 유효 데이터 (map v2.2, Vis/MinInliers 30)
| 회차 | 결과 | 비고 |
|---|---|---|
| P7 #1 | 성공 33.4s (B) | 오차 4cm |
| P7 #2 | 성공 0.0s (A) | 즉시 수렴 |
| P7 #3 | 미수렴 (60s) | 유효 — 어려움 지점다운 결과 |
| P1 #1 | 성공 0.0s (A) | 기준값 지점 |

무효 처리: P7 C1 #1(16:31, v2.1) — 펜스 제거 후 재매핑 전이라 환경≠지도
(`invalid-env-mismatch`). v1 데이터 전체는 지도 유실로 폐기(`*_mapv1_lost.csv`).

### 오늘 배운 것 (다음 세션 필독)
1. **환경이 바뀌면 즉시 이어매핑**: 펜스 제거 → P7에서 후보 154회 전부 거절
   (매칭은 되는데 실물이 달라 기하검증 탈락). 5분 이어매핑으로 해결, 33s 수렴으로 회복
2. **자율 이동(Nav2) 3회 실패 → 플랜 B 확정**: 지점 간 이동은 조이스틱,
   측정만 auto_trial `--no-nav`로. 원인 2가지: ① 책상 반복 무늬 가짜 루프
   클로저(MinInliers 30으로 완화, 근절은 안 됨) ② launch 재시작 후 액션
   결과 응답 유실(Nav2는 성공했는데 스크립트만 타임아웃)
3. **장시간 대기 중에도 믿음이 점프할 수 있음**: P1에서 10분 대기 중 P7로
   오인 (0.7m 가드가 차단). 회차 사이 간격을 길게 두지 말 것
4. **pkill 자기참조 함정**: 원격 명령 문자열에 죽일 프로세스 이름이 그대로
   들어가면 pkill이 자기 ssh를 죽임 → 패턴에 `[l]` 브래킷 쓰거나 정리/실행
   ssh를 분리할 것
5. **재부팅 직후 launch를 바로 올리지 말 것**: 카메라(Orbbec)가 초기화
   실패(`Failed to setup devices`)하면 relaunch로도 안 살아남 → USB 재플러그
   ※ 09-01 재플러그 후 확인: 같은 에러가 로그에 한 번 떠도 그 뒤 스트림이
   정상일 수 있음. 판정은 로그가 아니라 **토픽 수신**으로
   (`timeout 8 ros2 topic echo /camera/color/image_raw --field header.stamp.sec`)

### 다음 세션 재개 절차 (남은 것: P1 2회, P2 3회, P3 3회 → 이후 복도 P4~P6)
1. 로봇을 임의 지점(P1 권장) 근처에 두고:
   `ros2 launch wego limo_rtabmap_navigation_camera_only.launch.py rviz:=false nav_map:=/home/wego/maps/rtab_camera_nav.yaml`
2. 수렴 확인 후, 지점마다: 조이스틱으로 근처 이동(테이프 정밀 정렬 불필요 —
   스냅샷이 그 회차의 정답) → `python3 ~/reloc_test/auto_trial.py --point P1
   --condition C1 --trial 2 --no-nav --map v2.2 --note joytransit`
3. 실시간 로그: `tail -f <로그파일> | grep -v RTPS` / 결과: `tail -f ~/reloc_test/results.csv`

## 2.7 2026-09-01 세션 기록 — 테이프 정답 전환 + 실습 구역 재매핑 (C1 7/21회)

### 확보한 유효 데이터 (truth_src=points)
| 회차 | 지도 | 결과 | 비고 |
|---|---|---|---|
| P1 #1~#3 | v2.2 | **3/3 성공, 전부 Phase A** (0.0/0.0/1.5s, 오차 3~4cm) | 기준값 지점 |
| P7 #1 | v2.3 | 성공 0.0s (A), 오차 9cm/7° | 재매핑 직후 |
| P7 #2 | v2.3 | 미수렴 (60s) | |
| P7 #3 | v2.3 | 미수렴 + **snapjump4.15m** | 시작 전 믿음이 P1으로 점프해 있었음 |

C1 진행 (14:40 기준): **P1 3/3, P2 0/3, P4 1/3, P7 1/3 = 5/12 (42%)**. **P3 생략 결정(사용자, 시간 제약)**:
P2가 동일 계열(특징점 빈곤 실패)을 이미 대표하므로 정보 이득이 적음.
P4 완료 (1/3: #1 즉시 성공/13cm, #2 같은 자세인데 미수렴 — cold start
확률성의 직접 증거, #3 미수렴). P5는 보류(사용자), P6 미정.
발견: 정지 20초에 올바른 방향을 보는 게 승부처. 회전 중엔 모션 블러로
거의 못 잡음 → 실패가 실패를 부름(방향 복귀 불가 → 다음 회차 Phase A 낭비).
남은 것: (선택) P5·P6 → 저녁 C2 (P1·P2·P7 권장) → C3 → C4.
저장 완료: results.csv 동기화 + results_backup_20260901.csv,
report_table.md / report_map.png 갱신 (tools/make_report.py로 재생성 가능).
P2 0/3 참고: 3회 모두 회전 스윕(2바퀴+) 포함 미수렴 — 방향 문제 아님.
당시 카메라 장면 p2_view_c1_fail.jpg (흰 벽·흰 가전·바닥, 특징점 빈곤).
스팟 체크: v2.3 P1 오차 7.4cm/0.2° (spotcheck-after-cleanup-remap, trial 0).

### 사건 — 실습 구역 정리로 환경≠지도 재발
8/31 저녁 실습 구역 정리(펜스 등) → P7 시야가 v2.2와 불일치. 증상은 어제
펜스 때와 동일: 후보는 계속 나오나 기하검증 전멸(inliers 0~19/30), 90초
회전에도 미수렴. **이어매핑(v2.3, 2098→2819노드)으로 해결**, 직후 P7 즉시
수렴. → "환경 바뀌면 이어매핑" 원칙 재확인. 좌표계 유지 확인(P1 스팟 체크).

부수 사고: P7 테이프가 정리 중 유실 → Nav2로 구좌표 부근 이동 후 재설치,
좌표 재등록 (0.1절 위 "P7 재등록" 참고).

### P7 분석 — MinInliers 30이 만든 경계선
미수렴 회차의 로그: 매칭 자체는 많음(matches 50~105, 오늘 재매핑한 노드들과)
인데 inliers 10~20/30으로 문턱 바로 아래에서 반복 탈락. **기본값(20)이었다면
통과했을 회차들**임. 그러나 MinInliers 30은 책상 무늬 가짜 루프 클로저 대책
(8/31)이고, 30으로도 P1↔P7 혼동이 재발함(#3 snapjump). 낮추면 오탐이 늘
것이므로 **30 유지**. P7 성공률이 낮게 나오는 것은 파라미터 트레이드오프의
정직한 결과로 보고서에 그대로 쓴다.

### P1↔P7 시각적 혼동(perceptual aliasing) 누적 기록 — 결과물 소재
1. 8/31 17:55 — 로봇 P1, 믿음 P7 (4m 점프, 당시 가드가 측정 차단)
2. 9/1 13:43 — 로봇 P7, 믿음 P1 (snapjump4.15m, 기록됨)
양방향으로 재현됨. 실패 지점 지도에 오탐 사례로 표시할 것.

### 스크립트 변경 (auto_trial.py, 이 세션에서 배포)
- `--truth points|snap` (기본 points) + 스냅샷 병행 기록 + snapjump 자동 표시
- results.csv 스키마 자동 이행 (기존 행 truth_src=snap 백필)
- **launch 종료를 노드 단위로 검증** — 부모만 죽여 자식이 고아로 남던 버그
  수정 (limo_base 3중 실행 → 시리얼 충돌 사고)
- `--no-nav` 배치: 사전 수렴 불요, 재수렴 실패해도 배치 계속
- **방향 복귀(align_yaw)**: 회전 후 테이프 방향 자동 복귀. 복귀 불가 시
  해당 회차 위치만 판정 + note `+yawfree`
- `--where` (현재 믿음 vs 등록 지점), 배치 재개 문법 `"P1:2-3"`

### 다음 세션 주의
- **--map v2.3** 사용 (2.6절의 v2.2 재개 절차는 구버전)
- P1 데이터(v2.2)와 P7 데이터(v2.3) 혼재는 정당함: 정리 구역은 P1 시야 밖,
  책상 구역은 두 지도에서 동일. results.csv의 map 열로 추적 가능
- 배터리 11.7V까지 떨어짐 → 충전 중. C2(다른 시간대) 측정과 겸할 것
- RC 모드(SWB) 주의: /cmd_vel 무시 + 스틱 드리프트로 저절로 회전함.
  측정·Nav2 중엔 반드시 명령 모드

## 2.8 2026-09-02 세션 기록 — 의자 사건 + C3(소등) 12회 완료

### 사건 — 의자 "살짝" 이동만으로 기준 지점 사망 (환경≠지도 3번째 재발)
불 켠 P1 스팟 체크 2연속 미수렴 (어제는 3/3 즉시 성공이던 지점).
로그 signature는 앞선 두 번(펜스·구역 정리)과 동일: matches 80~124로 후보는
계속 나오는데 inliers 0~25/30으로 기하검증 전멸. 카메라 화면(p1_view_c1_0902.jpg)
확인 결과 근거리 의자 다리가 시야 절반을 지배 — 의자들이 밤사이 조금씩 이동.
**의자 몇 개가 살짝 움직인 것만으로 기준 지점이 죽는다** = 과제 질문
("지도를 언제까지 믿을 수 있나")에 대한 가장 직접적인 답.

해결: 검증된 절차대로 5분 이어매핑 → **v2.4 (3250노드)**, save_master 완료
(구 master는 rtab_camera_master.db.20260902_125625.bak). 직후 스팟 체크
0.4s / 8cm / 10° 성공. 좌표계 유지 확인(끄기 직전 tf가 P1 테이프와 8cm/9° 일치).

### C3 (소등·주간) 12회 결과 — map v2.4, 판정 truth=points
| 지점 | C3 | 비고 |
|---|---|---|
| P1 | 3/3 (전부 Phase A, 8cm) | 소등 영향 없음 |
| P2 | 1/3 (#1 즉시 성공 2cm!) | C1 0/3보다 오히려 좋음 |
| P4 | 1/3 (#1 즉시 성공 7cm) | C1과 동일 패턴 |
| P5 | 1/3 (#1 즉시 성공 6cm) | 복도 조명 잠깐 소등하고 측정 |
| P7 | 0/3 (3회 전부 snapjump) | P1↔P7 오인 2회 추가 |

**헤드라인: C1 5/12 (42%) ≈ C3 6/15 (40%).** 주간 소등(자연광 잔존 + 자동
노출 보정, c3_p1_view_0902.jpg 참고)은 성공률을 깎지 않는다. 성패를 가르는
것은 조명이 아니라 ① 지점의 특징점 품질 ② cold start 확률성 ③ perceptual
aliasing. 조건 정의는 "소등(주간, 창측 자연광 잔존)"으로 보고서에 명시.

### 조건 축소 결정 (박사님 구두 지침, 2026-09-02)
"조도 정도의 조건만 다르게 해도 된다" → C2(다른 시간대)·C4(배치 변경) 생략.
C4는 사실상 오늘 오전 의자 사건이 자연 실험으로 대체 (배치 변경 → 실패 →
이어매핑 필요까지 실측됨).

### 미완 (시간·배터리 제약, 사용자 결정)
- **P5 C1 미측정**: C3(1/3)까지 측정한 뒤 C1 3회를 돌리려는 시점에 카메라
  고장(아래) + 배터리 소진이 겹쳐 중단. C3만 있는 지점으로 기록
- P3 생략(어제 결정 유지)·P6 보류
- P7 C3의 snapjump 좌표 중 1회는 복도 쪽(-11.3, 11.8) — P6 방향 오인 사례

### 사건 — Orbbec 카메라 초기화 실패 (14:20~, 미해결 상태로 종료)
P5 C3 배치 종료(14:20) 직후부터 relaunch마다
`Failed to setup devices: Request failed, device response with unknown error!`
3회 연속. lsusb에는 장치가 보임. 어제 기록된 패턴과 동일 — **relaunch로
안 살아나며 USB 재플러그 필요**. 로봇을 끄면서 종료했으므로 **다음 기동 시
카메라 토픽 수신부터 확인할 것** (재부팅으로 풀릴 수도, 재플러그가 필요할
수도 있음):
`timeout 8 ros2 topic echo /camera/color/image_raw/compressed --field header.stamp.sec`

### P1↔P7 오인 누적 (도합 4회, 양방향)
1. 8/31 17:55 로봇P1→믿음P7 / 2. 9/1 13:43 로봇P7→믿음P1
3. 9/2 13:36 로봇P7→믿음P1 / 4. 9/2 13:39 로봇P7→믿음P1

### 데이터 파일
- results.csv (로봇↔맥북 동기), results_backup_20260902.csv
- report_table.md / report_map.png (make_report.py로 재생성)
- 시연 명령어: demo_commands.md (신규)

## 3. 조건 (PPT 19p)

| 조건 | 내용 | 역할 |
|------|------|------|
| C1 | 매핑과 같은 시간대, 같은 배치 | 기준값. 이게 낮으면 지도부터 다시 |
| C2 | 다른 시간대 (창가 채광 변화) | 조명 변화(자연광) |
| C3 | 조명 꺼짐 | 최악 조건. 특징점 급감 |
| C4 | 물건 배치 변경 (의자·파티션 이동) | 외관 변화 |

- 반복: 지점×조건당 **3회** (걸림돌 3번). 5지점 × 4조건 × 3회 = 60회
  회당 ~2분이면 실측 약 2~3시간 → 2일로 나눠도 됨 (C2는 어차피 다른 시간대)

## 4. 1회 측정 절차

1. 로봇을 지점 테이프에 맞춰 놓는다 (전원 켠 채, 스택만 재시작)
2. `ros2 launch wego limo_rtabmap_navigation_camera_only.launch.py` 실행
   (initial_pose 주지 않음 — 스스로 찾는지가 측정 대상)
3. 기록 스크립트 시작 → 첫 `/rtabmap/localization_pose` 수신 시각·자세 자동 기록
4. Phase A: 20초 정지 대기 → 미수렴 시 Phase B: 제자리 저속 회전 (60초까지)
5. 수렴하면 rtabmap_viz의 Loop closure detection 화면 캡처 (사후 검증용)
6. 기록표 기입, 스택 종료, 다음 회차

※ 3번은 스크립트로 자동화 (`~/reloc_test/relocation_logger.py`, 로봇에 배치됨).
   수동 스톱워치보다 정확하고, 60회 반복에서 사람 실수를 없앤다.

**중요 — 정답 등록 회차(--save-truth)는 측정 데이터가 아니다.**
자기 자신과 비교해 오차가 무조건 0이고, 스택이 이미 수렴한 상태(warm start)라
수렴 시간도 비교 불가. note=truth-reg 로 자동 표시되며 통계에서 제외한다.
→ **진짜 측정 회차는 반드시 navigation launch를 새로 시작한 뒤(cold start) 실행.**

## 5. 기록표 양식

```
날짜/시각, 지점, 조건, 회차, 성공여부(성공/미수렴/오탐), 수렴시간(s),
cov_x, cov_y, 매칭 키프레임 ID, 비고(조도 등)
```
CSV 한 장. 이걸로 조건별 성공률 표 + 실패 지점 지도 표시(과제 결과물) 생성.

## 6. 결과물 (PPT 17p)

1. 조건별 × 지점별 성공률 표
2. 실패 지점을 지도 위에 표시한 그림 (실패가 몰린 곳 = 카메라 SLAM 한계 지점)
3. (보너스) 같은 지점에서 LiDAR+AMCL(full_v9)은 성공하는데 카메라는 실패하는
   사례 — 두 방식 비교로 결론이 선명해짐

## 미확정 — 현장에서 정할 것

- [x] 판정 기준: 정답 대비 0.5 m / 30°, 2단계(20초 정지 + 60초 회전) — 확정
- [ ] 기준 지도 구역 (강의실만? 복도 포함?) — 매핑 당일 결정.
      강의실만으로 시작하고, 지도 품질이 좋으면 복도 확장 검토
- [ ] 측정 지점 확정 (매핑 후 지도 보고)
